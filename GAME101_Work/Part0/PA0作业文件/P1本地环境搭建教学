原始pa0中配置虚拟机环境比较复杂且很多人会卡在打不开虚拟机等各种复杂情况
因为系统环境不同也会导致虚拟机的使用可能出现版本冲突警告等等
总而言之，我们这里就是想通过在本地Windows系统下配置Opencv和Eigen的C++环境以避免大家卡在第一关
因为不同系统环境下的编译结果不同，你在网上下载到的编译好的opencv包可能并不能被立马使用
本教程面向保姆级手把手带学教会你如何完美配置oepncv和eigen环境


>>>>>>>>>>>Opencv的环境配置流程<<<<<<<<<<<<

一、准备VScode C++基础环境
打开VScode，没有的在微软商店或者官网下载即可
然后在左侧的插件库里搜索C++，随便下载即可，建议下载微软提供的C/C++插件
接着安装Code Runner插件
然后VScode就暂时搁置

二、MinGW环境配置
打开MinGW官网的下载指导页 ： https://www.mingw-w64.org/downloads/
找到你对应的系统
这里以Windows系统为例
进入下载页面： https://github.com/niXman/mingw-builds-binaries/releases
下载的版本：x86_64-16.2.0-release-posix-seh-msvcrt-rt_v14-rev1.7z
这里版本号你可能看这个教程的时间不同，它的版本可能不与我相同，因此匹配对应的参数尾号即可
然后解压文件到任意文件夹下即可，记住解压后的文件夹路径
接着，打开电脑的搜索板块搜索系统变量
点击环境变量
在下方的系统变量栏内选中Path，然后点击编辑，新建
这个时候，把我们刚才解压好的mingw目录下的bin文件目录粘贴到此，例如：D:\mingw64\bin
！！！注意：这里的环境变量不是用户环境变量而是系统环境变量，是下面的那一个板块，注意看清楚
确认创建好后一路「确定」保存
然后，打开VScode，创建一个工作
在任意地方，这里以桌面为例，创建一个Hello.cpp文件
在VScode向这个文本输入测试代码:
#include<iostream>
using namespace std;
int main(){
    cout << Hello World! << endl;
    return 0;
}
按Ctrl+S保存当前文件
找到VScode右上角的三角形，点击CodeRunner或者按Ctrl + Shift + N
Vscode下面会跳出来一个输出窗口，如果正常打印输出了“Hello World!”就代表识别到了我们的MinGW编译器并使用
如果发现还不能，请检查是否配置好了系统环境变量，是否安装好了runnercode插件，重启vscode再检查
如果还不能，请跳转至末尾配置常见问题查看解决方案
然后再次用code runner编译那个cpp文件，这次就能够成功输出了，表示我们已经配置好了vscode的c++编译器

三、cmake安装
在https://cmake.org/download/下载cmake
下载：Windows x64 ZIP 下的files即可
得到zip压缩包解压到任意文件夹下
复制它目录下的bin文件夹路径
例如:D:\cmake-4.4.3-windows-x86_64\bin
用我们之前的方法，将它配置到系统环境变量中[也是bin文件路径，和这里例如后的路径一样]，方法一模一样这里不做赘述

四、检验与检查
按Win+R，输入cmd
输入 cmake --version
如果有跳出来信息说明cmake安装至当前系统环境中
输入 gcc --version
如果有信息跳出来显示gcc版本信息说明我们之前的mingw也安装成功了

五、opencv下载
在https://opencv.org/releases/下，找到windows对应的下载按钮下载最新版的opencv
我的是opencv5.0.0，也就是2026-8-19更新的版本
你也可以选择其他版本，其实都不影响实际学习
下载好opencv的安装器exe文件
打开，将opencv下载到任意指定文件夹下即可
安装好后，文件夹打开至opencv的目录，在build\x64文件夹下创建一个英文名文件夹
可以随便取名，最好是纯英文，比如MinGW，后续以此为例

六、cmakefile与oepncv编译
接下来来到最热血的环节，因为这里会让人血压高升仿佛置身外太空，容易气炸
为了减少我们查找问题方案日志的难度，我们这里推荐各位找到一个合适的agentAi辅助进行问题定位和方案提供
因为我没办法把所有的问题解决方案都告诉你
每个人的电脑环境不同，出现的问题不尽相同，使用Ai就是最好的解决办法
这里以trea solo调用deepseek为例
首先，找到我们之前下载cmake的地方，打开bin文件夹
比如：D:\cmake-4.4.3-windows-x86_64\bin
在bin下找到一个叫 cmake-gui.exe 的程序
打开它
在程序的顶部有三行输入栏，其中中间灰色不可输入，我们只需要编辑第一行和第三行
第一行需要选择opencv的resource文件夹，例如：D:/opencv/sources
第三行需要选择我们先前创建的文件夹，例如：D:/opencv/build/x64/MinGW
然后点击configure
弹出小窗口，Specify the generator for this project选择 MinGW Makefiles
下面的四个选项选择第二个：Specify native compilers
点击next跳转到下一页
这里需要选择编译器
对C，我们选择gcc.exe，例如：D:\Mingwlocal\mingw64\bin\gcc.exe
对C++，我们选择g++.exe，例如：D:\Mingwlocal\mingw64\bin\g++.exe
点击finish就会自动创建files
如果此处弹出指示invilid信息，则说明编译器和opencv可能对应不上新旧版本导致编译出无法识别的文件
接下来，提示需要勾选编译配置
勾选 WITH_OPENGL
勾选 ENABLE_CXX11
勾选 WITH_MSMF
不勾选 WITH_IPP
不勾选 ENABLE_PRECOMPILED_HEADERS
点击configure，完毕后点击generate生成Makefile
此时我们就需要开始编译
按win+r输入cmd打开终端
然后用命令跳转至我们先前创建的文件夹
输入 cd \d D:/opencv/build/x64/MinGW     [改成你的地址]
然后再输入mingw32-make -j 8
注意 -j后面的数字是线程，如果你的计算机配置较高可以调高，低则调低
等待编译
此时通常都会很不幸地报错终止
我们无需关闭终端，而是把错误信息复制粘贴到ai的聊天窗口
然后告诉Ai：
“我现在正在通过MinGW编译用cmake创建好的opencv的相关文件，
在上述过程中遇到编译错误信息，请帮我定位查找编译错误信息的信息源，
然后再根据信息错误分析原因，最终给出具体方案
值得注意的是重点分析我的cmake编译配置设置是否可以修改达到更合理的编译结果
而且还要尽可能避免对源文件代码等的刻意修改删减”
然后几轮编译失败下来,多次与AI对话他就会知道该如何解决了
注意，不要轻易让AI帮你修改代码，文件，让他给具体操作方案由你来做
到最后编译错误处理好了之后，我们输入代码：
mingw32-make install
等待装配完成，我们回到先前创建的文件夹D:/opencv/build/x64/MinGW  
找到bin文件夹，复制路径，例如：D:\opencv\build\x64\MinGW\bin
然后根据我们先前配置环境变量的方法，将这个路径也配置到环境中去

七、opencv的coderunner -l指路配置
打开vscode
按Ctrl+,
接着搜索coderunner
进入第一个json的配置文件
然后找到cpp所在位置
将后面的内容替换为：
"cpp": "chcp 65001 >nul && cd $dir && g++ $fileName -o $fileNameWithoutExt -I D:/opencv/build/x64/MinGW/install/include -L D:/opencv/build/x64/MinGW/install/x64/mingw/lib -lopencv_core500 -lopencv_imgproc500 -lopencv_imgcodecs500 -lopencv_highgui500 -lopencv_videoio500 -lopencv_video500 -lopencv_photo500 -lopencv_features500 -lopencv_flann500 -lopencv_geometry500 -lopencv_ptcloud500 -lopencv_stereo500 -lopencv_stitching500 -lopencv_objdetect500 -lopencv_calib500 && $dir$fileNameWithoutExt",
然后在vscode里创建一个cpp文件
输入
#include <opencv2/opencv.hpp>  
#include <opencv2/core/core.hpp>  
#include <opencv2/highgui/highgui.hpp>  
#include <opencv2/imgproc.hpp>  
#include<iostream>  
using namespace std;  
using namespace cv;  
int main()  
{  
    Mat image = Mat::zeros(300, 600, CV_8UC3);  
    circle(image, Point(300, 200), 100, Scalar(25, 110, 288),-100);  
    circle(image, Point(400, 200), 100, Scalar(255, 123, 127), -100);  
    imshow("Show Window", image);  
    waitKey(0);  
    return 0;  
}  
并保存，然后点三角形的coderunner进行编译运行
如果成功弹出一个小窗口显示两个叠加圆形则代表opencv配置成功
但vscode可能会显示警告线
你只需要配置以下内容即可，内容很简单，交给AI执行即可
VS Code 里按 Ctrl+Shift+P，输入：
Preferences: Open User Settings (JSON)
滑倒末尾，把原来的gleam改为下面的形式

        "gleam": "gleam run -m $fileNameWithoutExt"
    },
    "C_Cpp.default.includePath": [
        "${workspaceFolder}/**",
        "D:/opencv/build/x64/MinGW/install/include"                 <-----------注意这里是你的MinGW/install/include的地址
    ],
    "C_Cpp.default.cppStandard": "c++17",
    "C_Cpp.default.compilerPath": "D:/Mingwlocal/mingw64/bin/g++.exe",            <--------------这里是你的g++.exe的地址
    "C_Cpp.default.intelliSenseMode": "windows-gcc-x64"       
}
把上面的内容喂给AI，让他帮你把VSCODE的这个全局状态配置好即可


>>>>>>>>>>>Eigen库的挂载配置流程<<<<<<<<<<<<

八、线性代数运算库Eigen配置
打开https://libeigen.gitlab.io/
在Download下的“The latest stable release is Eigen 5.0.0. Get it here: tar.bz2, tar.gz, zip.”中
点击zip
然后解压到一个任意路径位置，记住它
例如：D:\eigen-5.0.0
然后改两个地方
一、在原来的gcc配置json中加入一个-l指向eigen
二、在全局设置json中加入eigen路径
这两点你都可以交给AI快速完成
当然你也可以手搓：
S Code 中按 Ctrl+Shift+P
输入 Preferences: Open User Settings (JSON) 
打开的json当中
定位到 "code-runner.executorMap" 下面的 "cpp" 这一项
在已有的 OpenCV -I 后面插入 -I D:/LinearAlgebraEigen/eigen-5.0.0
也就是D:/opencv/build/x64/MinGW/install/include -I D:/LinearAlgebraEigen/eigen-5.0.0
实际改动后变为：
"cpp": "chcp 65001 >nul && cd $dir && g++ $fileName -o $fileNameWithoutExt -I D:/opencv/build/x64/MinGW/install/include -I D:/eigen-5.0.0 -L D:/opencv/build/x64/MinGW/install/x64/mingw/lib -lopencv_core500 -lopencv_imgproc500 -lopencv_imgcodecs500 -lopencv_highgui500 -lopencv_videoio500 -lopencv_video500 -lopencv_photo500 -lopencv_features500 -lopencv_flann500 -lopencv_geometry500 -lopencv_ptcloud500 -lopencv_stereo500 -lopencv_stitching500 -lopencv_objdetect500 -lopencv_calib500 && $dir$fileNameWithoutExt",

还是这个json
滑倒末尾，找到"C_Cpp.default.includePath"
改为
    "C_Cpp.default.includePath": [
        "${workspaceFolder}/**",
        "D:/opencv/build/x64/MinGW/install/include",
        "D:/eigen-5.0.0"           <------------这里改为eigen的目录地址
    ],

到此按Ctrl+S保存，然后重启VScode
你的代码就完全可以编译运行opencv和eigen了
这样就能完美运行一份作业了
值得注意的是
作业源码给到的是Linux系统下的cpp代码
如果不修改可能会导致编译错误
建议用AI改正为Windows系统下的代码修正，这样就能完美运行了

九、其他常见问题和方案
一般都能和AI对话解决

1.runner code配置问题
如果runner code无法识别到当前系统的编译器
就请检查是否下载好编译器，路径是否正确，是否成功配置环境变量
按照上面的教程重新检查
